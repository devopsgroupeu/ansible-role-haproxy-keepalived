---
- name: Install HAProxy via the selected method
  ansible.builtin.include_tasks: "haproxy_install_{{ haproxy_install_method }}.yml"

- name: Create certificate directory
  ansible.builtin.file:
    path: /etc/haproxy/certs
    state: directory
    owner: root
    group: haproxy
    mode: "0750"

- name: Deploy SSL certificates
  ansible.builtin.copy:
    src: "{{ haproxy_ssl_cert_src }}"
    dest: /etc/haproxy/certs/
    owner: root
    group: haproxy
    mode: "0640"
  when: haproxy_ssl_cert_src | length > 0

- name: Create HAProxy socket directory
  ansible.builtin.file:
    path: "{{ haproxy_socket_dir }}"
    state: directory
    owner: root
    group: haproxy
    mode: "0750"

- name: Create HAProxy admin socket directory
  ansible.builtin.file:
    path: "{{ haproxy_admin_socket_dir }}"
    state: directory
    owner: root
    group: "{{ haproxy_group }}"
    mode: "0750"

- name: Copy HAProxy configuration
  ansible.builtin.template:
    src: haproxy.cfg.j2
    dest: "{{ haproxy_config_dir }}/haproxy.cfg"
    mode: "0644"
    validate: "{{ haproxy_binary }} -f %s -c"
    backup: true
  notify:
    - Reload haproxy
    - Cleanup haproxy config backups

- name: Configure the rsyslog configuration file to accept UDP syslog reception
  ansible.builtin.template:
    src: haproxy-rsyslog.conf.j2
    dest: "/etc/rsyslog.d/haproxy.conf"
    mode: "0644"
  notify: Restart rsyslog

- name: Ensure HAProxy service is running
  ansible.builtin.systemd_service:
    name: haproxy
    state: started
    enabled: true

- name: Wait for HAProxy to be ready
  ansible.builtin.wait_for:
    port: "{{ haproxy_stats_port }}"
    delay: 2
    timeout: 30
  when: haproxy_stats | bool

- name: Verify HAProxy is responding
  ansible.builtin.uri:
    url: "http://127.0.0.1:{{ haproxy_stats_port }}{{ haproxy_stats_page_uri }}"
    user: "{{ haproxy_stats_page_user }}"
    password: "{{ haproxy_stats_page_pass }}"
    force_basic_auth: true
    status_code: 200
  when: haproxy_stats | bool
  retries: 3
  delay: 2
  register: haproxy_stats_check
  until: haproxy_stats_check is succeeded

- name: Check HAProxy process is running
  ansible.builtin.command: pgrep -x haproxy
  register: haproxy_process
  changed_when: false
  failed_when: haproxy_process.rc != 0
