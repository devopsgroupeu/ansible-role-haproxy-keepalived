---
galaxy_info:
  author: DevOpsGroup
  company: DevOpsGroup s.r.o
  description: >
    Production-ready Ansible role for installing and configuring HAProxy with Keepalived
    in high-availability mode. Supports package install (default) or source compilation,
    internet-connected or air-gapped environments.

  license: Apache-2.0
  min_ansible_version: "2.19"

  role_name: haproxy_keepalived
  namespace: devopsgroupeu

  galaxy_tags:
    - loadbalancer
    - haproxy
    - keepalived
    - highavailability
    - ha
    - vrrp
    - failover
    - proxy
    - reverseproxy
    - networking
    - clustering
    - vip

  platforms:
    - name: Ubuntu
      versions:
        - noble
    - name: Debian
      versions:
        - bookworm
        - trixie
    - name: EL
      versions:
        - "9"

dependencies: []
